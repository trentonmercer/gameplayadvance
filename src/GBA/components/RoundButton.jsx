import React from 'react'

const ButtonRound = (props) => {
  return (
    <div id="button_" type="button" class="abutton btn btn-lg btn-default btn-circle pull-right gbabuttons">{props.type}</div>
  )
}

export default ButtonRound
