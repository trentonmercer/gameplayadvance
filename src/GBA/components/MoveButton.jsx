import React from 'react'

/* MoveButton


*/

const MoveButton = (props) => {
  return (
    <span id="button_6" class="btn btn-default btn-direction up gbabuttons">{props.direction}</span>
  )
}

export default MoveButton;
